\begin{figure}[t]
\centering
\includegraphics[width=0.85\columnwidth]{figures/layerwise_alpha_ablation.pdf}
\caption{Per-layer constant-$\alpha{=}1$ ablation on Qwen2.5-0.5B (a single layer ablated at a time, all others intact). Perplexity vs.\ ablated layer index; the dashed line is the unmodified baseline ($\approx16.8$). Routing is most load-bearing in the middle and final layers; early-layer ablations leave perplexity within a small constant factor of baseline.}
\label{fig:layerwise_alpha}
\end{figure}

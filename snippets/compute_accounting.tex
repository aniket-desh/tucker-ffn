At the matched-parameter LM config ($d{=}512$, $L{=}8$), SwiGLU and Tucker-core FFNs have $F_S \approx 3.67e+07$ and $F_T \approx 3.67e+07$ forward FLOPs per token respectively, with measured throughput $769$ and $1818$ tokens/sec on a single CPU; Tucker is comparable in compute despite matched parameters.

At the matched-parameter LM config ($d{=}512$, $L{=}8$), SwiGLU and Tucker-core FFNs have $F_S \approx 3.67e+07$ and $F_T \approx 3.67e+07$ forward FLOPs per token respectively, with measured throughput $112496$ and $75009$ tokens/sec on a single A40; Tucker is comparable in compute despite matched parameters.

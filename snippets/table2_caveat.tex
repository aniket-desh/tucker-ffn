Single seed; within-run std of val.\ loss over the last 10 checkpoints is $\approx0.014$ (SwiGLU) and $\approx0.014$ (Tucker), comparable to or larger than the 0.005-nat gap, so the table should be read as a sanity check rather than a superiority claim.

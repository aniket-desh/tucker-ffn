\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_pairing_permutation.png}
\caption{Per-layer perplexity under random permutations of one trained SwiGLU layer. Joint $\pi_G{=}\pi_U$ breaks the same-index W--G coupling; $\pi_U$-only is a control that breaks $U$ pairing alone. The gap quantifies how binding the same-index coupling is.}
\label{fig:fig_pairing_permutation}
\end{figure}

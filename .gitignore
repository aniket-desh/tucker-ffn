__pycache__/
*.pyc
.DS_Store

# trillium workflow — generated job scripts (regenerated by their parent
# generator on every run; never edit by hand)
slurm/_*.sh

# experiment outputs are produced on the cluster and pulled back via
# slurm/fetch_results.sh — they should not be committed
results/
logs/

# runpod workflow — uv-managed venv + per-pod secrets
.venv/
.env
*.egg-info/
